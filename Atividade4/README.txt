Exercício 1.
a. Crie as classes solicitadas.
-Animal = atributos(nome,peso), metodos (gets e sets encapsulados);
-Peixe (herda animal) = atributos (habitat), metodos (gets e sets encapsulados), saidas (nome, peso e habitat);
-Cachorro (herda animal) = atributos (raca), metodos (gets e sets encapsulados), saidas (nome, peso e raca);
b. Faça o relacionamento (herança) entre as classes.
c. Defina a saída dos dados (toString()) nas classes indicadas. A classe onde tem a
indicação é onde estará a saída – toString().
d. Faça a classe de teste e execute.

Exercício 2.
a. Crie as classes solicitadas.
-Pessoa = atributos (nome, idade, genero), metodos (gets e sets encapsulados)
Cliente (herda Pessoa) = atributos (valorDivida, anoNascim), metodos (gets e sets encapsulados);
Empregado (herda Pessoa) = atributos (salario, matricula), metodos (gets e sets encapsulados, valorInss()),
Gerente (herda Empregado) = atributos (nomeGerencia), metodos (gets e sets encapsulados), saida (nome, idade, nome da gerencia, valor do INSS do gerente);
Vendedor (herda Empregado) = atributos (valorVendas, qntVendas), metodos (gets e sets encapsulados), saida (nome, salario, valor das vendas e quantidade);
b. Faça o relacionamento (herança) entre as classes.
c. Defina a saída dos dados (toString()) nas classes indicadas. A classe onde tem a
indicação é onde estará a saída – toString().
d. Faça a classe de teste e execute.
e. O método valorInss() tem a fórmula (salário * 11%).